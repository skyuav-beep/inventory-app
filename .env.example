## Root environment defaults (copy to .env)

# PostgreSQL connection string
DATABASE_URL=postgresql://inventory:inventory@localhost:5432/inventory

# JWT secret used by NestJS API
JWT_SECRET=change-me

# Optional: Telegram bot defaults (UI에서 재설정 가능)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Seeder admin account (apps/api/prisma/seed.ts)
ADMIN_SEED_EMAIL=admin@example.com
ADMIN_SEED_NAME=Admin
ADMIN_SEED_PASSWORD=ChangeMe123!

# Optional: global rate limit overrides
THROTTLE_DEFAULT_TTL=60
THROTTLE_DEFAULT_LIMIT=120
